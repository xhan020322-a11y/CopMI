% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/margins.R
\name{copmi_fit_margins}
\alias{copmi_fit_margins}
\title{Fit all margins under a log-normal assumption or by BIC selection}
\usage{
copmi_fit_margins(
  x,
  margin_mode = c("sd_shift", "normal"),
  input_scale = c("log", "raw"),
  shift_k = 3,
  margin_candidates = c("norm", "logis", "lnorm", "gamma", "weibull", "exp", "invgauss",
    "gengamma", "llogis", "lomax", "burr"),
  optim_methods = c("L-BFGS-B", "Nelder-Mead")
)
}
\arguments{
\item{x}{A \code{\link[=make_lod_data]{make_lod_data()}} object.}

\item{margin_mode}{\code{"normal"} if the user assumes normality after logging,
or \code{"sd_shift"}/\code{"select"} for heterogeneous candidate selection. This is a
user-specified modelling assumption, not an automatic normality test.}

\item{input_scale}{\code{"log"} (default) if values and cutoffs are already logged;
\code{"raw"} to apply natural log internally to strictly positive values and
finite cutoffs. Imputations are returned on this supplied scale.}

\item{shift_k}{Positive SD-shift multiplier, used only for selection.}

\item{margin_candidates}{Candidate family names, used only for selection.
See \code{\link[=copmi_fit_margin]{copmi_fit_margin()}}. In normal mode the sole candidate is \code{norm}.}

\item{optim_methods}{Ordered optimization methods. The default first tries
\code{"L-BFGS-B"}, then \code{"Nelder-Mead"} on error, nonconvergence, or invalid
estimates/log-likelihood/BIC. Both optimize the same left-censored
likelihood on log-transformed positive parameters, without empirical
parameter bounds. Location parameters remain unconstrained. A single
method can be supplied for controlled comparisons.}
}
\value{
A \code{copmi_margins} list with \code{fits} (named list of \code{copmi_margin}
objects), \code{family_selected} (named character vector), \code{candidate_table} and
\code{optimization} (tables with a \code{variable} column), \code{input} (original data),
\code{analysis_data} (log-scale data), \code{work_data} (data used for fitting),
\code{shift} (\code{copmi_shift} or \code{NULL}), \code{margin_mode}, \code{input_scale}, and
\code{optim_methods}. No copula estimation or random imputation occurs here.
}
\description{
Separately choose the supplied data scale and the marginal assumption.
\code{margin_mode = "normal"} fits censored normal margins on the log scale.
\code{margin_mode = "sd_shift"} shifts log-scale values and selects a margin for
each column by BIC. The alias \code{"select"} is also accepted for the latter.
}
\examples{
margins <- copmi_fit_margins(nhanes_pah, margin_mode = "normal")
margins$family_selected
margins$fits[[1]]$parameters
selected <- copmi_fit_margins(nhanes_pah, margin_mode = "select",
                              margin_candidates = c("norm", "logis"))
selected$candidate_table
}
\seealso{
\code{\link[=copmi_shift]{copmi_shift()}}, \code{\link[=copmi_transform]{copmi_transform()}}, \code{\link[=copula_em_impute]{copula_em_impute()}}
}
