% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract.R
\name{diagnostics}
\alias{diagnostics}
\alias{diagnostics.copmi_mi}
\alias{diagnostics.copmi_copula}
\alias{diagnostics.copmi_margins}
\alias{diagnostics.copmi_margin}
\title{Extract fitting, optimization, and imputation diagnostics}
\usage{
diagnostics(object, ...)

\method{diagnostics}{copmi_mi}(object, ...)

\method{diagnostics}{copmi_copula}(object, ...)

\method{diagnostics}{copmi_margins}(object, ...)

\method{diagnostics}{copmi_margin}(object, ...)
}
\arguments{
\item{object}{A \code{copmi_mi}, \code{copmi_copula}, \code{copmi_margins}, or \code{copmi_margin}
object returned by the corresponding fitting function.}

\item{...}{Reserved; unused arguments raise an error.}
}
\value{
A named list. For imputation results: \code{converged}, \code{n_iter},
\code{Sigma_hat}, \code{Sigma_init}, \code{em_change_history}, \code{family_selected},
\code{bic_table}, \code{candidate_table}, \code{optimization}, \code{fallback_records},
\code{init_diagnostics}, \code{shift_anchor}, \code{shift_sd}, \code{shift_sd_source},
\code{input_scale}, and \code{sampling}. The fallback table has columns \code{method},
\code{stage}, \code{count}, and \code{detail}; \code{count} counts events, not necessarily
distinct rows or variables. Marginal objects return the relevant candidate
and optimization tables; copula objects return correlation and EM details.
}
\description{
Extract fitting, optimization, and imputation diagnostics
}
\details{
A successful Nelder-Mead retry remains visible in \code{optimization}
even if the final fit converges. The fallback table records marginal and initial-pair optimizer
retries, failed marginal candidates, and initial-matrix projection. EM or
sampling failures raise errors; they do not produce replacement values. Consult the per-attempt \code{message} and \code{warnings}
columns when diagnosing a failed candidate.
}
\examples{
# Use the first 100 rows for a quick help example; full data have 1330 rows.
dat <- make_lod_data(nhanes_pah$X_cens[1:100, ],
                     nhanes_pah$ind[1:100, ], nhanes_pah$cutoffs, scale = "log")
fit <- copula_em_impute(dat, margin_mode = "normal",
                         m = 1, max_iter = 100)
dx <- diagnostics(fit)
dx$converged
dx$optimization
dx$fallback_records
}
