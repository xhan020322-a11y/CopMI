% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract.R
\name{complete}
\alias{complete}
\alias{complete.copmi_mi}
\title{Extract completed data from a CopMI result}
\usage{
complete(object, action = "all", ...)

\method{complete}{copmi_mi}(object, action = "all", ...)
}
\arguments{
\item{object}{A \code{copmi_mi} object from \code{\link[=copula_em_impute]{copula_em_impute()}} or \code{\link[=copmi_impute]{copmi_impute()}}.}

\item{action}{One positive integer selecting an imputation, or \code{"all"},
\code{"mean"}, or \code{"long"}.}

\item{...}{Reserved; unused arguments raise an error.}
}
\value{
For an integer, an \code{n} by \code{p} matrix; for \code{"all"}, a list of \code{m}
matrices; for \code{"mean"}, an \code{n} by \code{p} elementwise average; for \code{"long"},
a data frame with \code{n * m} rows and columns \code{.imp} (1 through \code{m}), \code{.id}
(original row number), then the exposure variables in their original order.
}
\description{
Extract completed data from a CopMI result
}
\details{
The mean completion is a descriptive convenience. An analysis of
this average does not propagate between-imputation uncertainty. CopMI does
not pool downstream model estimates or standard errors. Use
\code{CopMI::complete()} if another package defines a function named \code{complete}.
}
\examples{
# Use the first 100 rows for a quick help example; full data have 1330 rows.
dat <- make_lod_data(nhanes_pah$X_cens[1:100, ],
                     nhanes_pah$ind[1:100, ], nhanes_pah$cutoffs, scale = "log")
fit <- copula_em_impute(dat, margin_mode = "normal",
                         m = 2, max_iter = 100)
dim(complete(fit, 1))
length(complete(fit, "all"))
dim(complete(fit, "mean"))
head(complete(fit, "long"))
}
