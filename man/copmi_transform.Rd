% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/latent.R
\name{copmi_transform}
\alias{copmi_transform}
\title{Transform fitted margins to latent Gaussian scores}
\usage{
copmi_transform(object)
}
\arguments{
\item{object}{A \code{copmi_margins} object from \code{\link[=copmi_fit_margins]{copmi_fit_margins()}}.}
}
\value{
A \code{copmi_latent} list with \code{Z} (numeric matrix of Gaussian scores),
\code{ind} (observation indicators), \code{z_lod} (latent cutoffs; \code{Inf} for absent
cutoffs), and \code{margins} (the supplied fitted margins). Dimensions and names
match the input data. Censored entries in \code{Z} are cutoff placeholders,
not observations. Both probability tails are evaluated on the log scale; no probability clipping is used.
}
\description{
Apply \code{qnorm(F_j(x))} using the selected fitted CDF for each variable.
}
\examples{
margins <- copmi_fit_margins(nhanes_pah, margin_mode = "normal")
latent <- copmi_transform(margins)
head(latent$Z)
latent$z_lod
}
\seealso{
\code{\link[=copmi_fit_copula]{copmi_fit_copula()}}
}
