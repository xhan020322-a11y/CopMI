% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/impute.R
\name{copula_em_impute}
\alias{copula_em_impute}
\title{Multiple imputation for left-censored exposure data}
\usage{
copula_em_impute(
  x,
  ind = NULL,
  cutoffs = NULL,
  m = 5,
  margin_mode = c("sd_shift", "normal"),
  max_iter = 100,
  tol = 1e-04,
  seed = 1,
  shift_k = 3,
  margin_candidates = c("norm", "logis", "lnorm", "gamma", "weibull", "exp", "invgauss",
    "gengamma", "llogis", "lomax", "burr"),
  ag = "gibbs",
  thinning = 2,
  verbose = FALSE,
  lyles_control = list(),
  input_scale = c("log", "raw"),
  optim_methods = c("L-BFGS-B", "Nelder-Mead"),
  allow_unconverged = FALSE,
  ...
)
}
\arguments{
\item{x}{A \code{copmi_lod_data} object, or a numeric matrix/data frame.}

\item{ind}{Observation indicator matrix, required for matrix/data frame \code{x}.}

\item{cutoffs}{Column cutoffs, required for matrix/data frame \code{x}.}

\item{m}{Positive integer number of completed data sets.}

\item{margin_mode}{\code{"normal"} if the user assumes normality after logging,
or \code{"sd_shift"}/\code{"select"} for heterogeneous candidate selection. This is a
user-specified modelling assumption, not an automatic normality test.}

\item{max_iter}{Nonnegative integer maximum number of EM iterations. Zero
returns initialization only and sets \code{converged = FALSE}.}

\item{tol}{Positive finite convergence tolerance: the maximum absolute
elementwise change in the correlation matrix must be smaller than \code{tol}.}

\item{seed}{Nonnegative integer seed, or \code{NULL} to use and advance the current
RNG stream. Explicit seeds restore the caller's RNG state on exit.}

\item{shift_k}{Positive SD-shift multiplier, used only for selection.}

\item{margin_candidates}{Candidate family names, used only for selection.
See \code{\link[=copmi_fit_margin]{copmi_fit_margin()}}. In normal mode the sole candidate is \code{norm}.}

\item{ag}{Sampling algorithm: \code{"gibbs"}, \code{"gibbsR"}, or \code{"rejection"}, passed
to \code{\link[tmvtnorm:rtmvnorm]{tmvtnorm::rtmvnorm()}}.}

\item{thinning}{Positive integer number of Gibbs steps per retained draw.
Ignored by the rejection sampler.}

\item{verbose}{Whether to print initialization and EM progress.}

\item{lyles_control}{Named list with optional \code{min_obs} (default 2), \code{maxit}
(2000 per optimizer), and \code{rho_max} (1) for pairwise censored-normal initialization.
Pairwise likelihoods try L-BFGS-B then Nelder-Mead. Failed pairs
raise errors. \code{rho_max < 1} explicitly restricts censored-pair fits;
fully observed pairs use their sample correlation.}

\item{input_scale}{\code{"log"} (default) if values and cutoffs are already logged;
\code{"raw"} to apply natural log internally to strictly positive values and
finite cutoffs. Imputations are returned on this supplied scale.}

\item{optim_methods}{Ordered optimization methods. The default first tries
\code{"L-BFGS-B"}, then \code{"Nelder-Mead"} on error, nonconvergence, or invalid
estimates/log-likelihood/BIC. Both optimize the same left-censored
likelihood on log-transformed positive parameters, without empirical
parameter bounds. Location parameters remain unconstrained. A single
method can be supplied for controlled comparisons.}

\item{allow_unconverged}{Allow exploratory draws from an unconverged or
initialization-only fit. Defaults to \code{FALSE}; \code{converged} remains \code{FALSE}.}

\item{...}{Reserved; unused arguments raise an error.}
}
\value{
A \code{copmi_mi} object as documented in \code{\link[=copmi_impute]{copmi_impute()}}. Each completed
matrix has the original dimensions, names, observed values, and input
scale. \code{converged} records EM convergence, not marginal optimizer status.
}
\description{
Run the complete pipeline: validate data, optionally log the input, fit
margins, transform to Gaussian scores, estimate copula correlation by EM,
and draw completed data sets on the supplied scale.
}
\details{
Use \verb{input_scale = "raw", margin_mode = "normal"} when positive raw
exposures are assumed log-normal; the package applies natural log and fits
censored normal margins. Use \code{input_scale = "log"} for data already logged.
Without the normal assumption, use \code{margin_mode = "sd_shift"} (or
\code{"select"}) for BIC selection on the shifted log scale. No normality test
silently decides the mode. Both modes use the ordered \code{optim_methods}.

Separate calls to \code{\link[=copmi_fit_margins]{copmi_fit_margins()}}, \code{\link[=copmi_transform]{copmi_transform()}},
\code{\link[=copmi_fit_copula]{copmi_fit_copula()}} with the same seed, and \code{\link[=copmi_impute]{copmi_impute()}} with its
default seed reproduce the one-call result. \code{max_iter = 0} is available
for initialization-only diagnostics, not a converged EM fit.
}
\examples{
# Use the first 100 rows for a quick help example; full data have 1330 rows.
dat <- make_lod_data(nhanes_pah$X_cens[1:100, ],
                     nhanes_pah$ind[1:100, ], nhanes_pah$cutoffs, scale = "log")
# Input is already on the log scale; assume normal log margins.
fit <- copula_em_impute(dat, margin_mode = "normal",
                         m = 2, max_iter = 100, seed = 1)
complete(fit, 1)
diagnostics(fit)$optimization
# Positive raw-scale data: transform internally and return raw-scale values.
raw <- make_lod_data(exp(dat$X_cens), dat$ind,
                     exp(dat$cutoffs))
fit_raw <- copula_em_impute(raw, input_scale = "raw", margin_mode = "normal",
                             m = 1, max_iter = 100, seed = 1)
head(complete(fit_raw, 1))
}
\seealso{
\code{\link[=make_lod_data]{make_lod_data()}}, \code{\link[=complete]{complete()}}, \code{\link[=diagnostics]{diagnostics()}}
}
