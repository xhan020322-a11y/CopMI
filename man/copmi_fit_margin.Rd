% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/margins.R
\name{copmi_fit_margin}
\alias{copmi_fit_margin}
\title{Fit and compare left-censored marginal distributions}
\usage{
copmi_fit_margin(
  x,
  ind,
  cutoff,
  candidates = c("norm", "logis", "lnorm", "gamma", "weibull", "exp", "invgauss",
    "gengamma", "llogis", "lomax", "burr"),
  optim_methods = c("L-BFGS-B", "Nelder-Mead")
)
}
\arguments{
\item{x}{Numeric vector of exposure values. Censored values may be \code{NA}.}

\item{ind}{Numeric or logical vector, with \code{1} observed and \code{0} censored.}

\item{cutoff}{One finite detection limit; \code{NA} if all values are observed.}

\item{candidates}{Unique candidate names: \code{norm}, \code{logis}, \code{lnorm}, \code{gamma},
\code{weibull}, \code{exp}, \code{invgauss}, \code{gengamma}, \code{llogis}, \code{lomax}, or \code{burr}.}

\item{optim_methods}{Ordered optimization methods. The default first tries
\code{"L-BFGS-B"}, then \code{"Nelder-Mead"} on error, nonconvergence, or invalid
estimates/log-likelihood/BIC. Both optimize the same left-censored
likelihood on log-transformed positive parameters, without empirical
parameter bounds. Location parameters remain unconstrained. A single
method can be supplied for controlled comparisons.}
}
\value{
A \code{copmi_margin} list containing \code{family}, \code{parameters} (named numeric
vector), \code{bic}, \code{loglik}, \code{optimizer}, \code{how}, \code{candidate_table} (one row per
candidate, including failure messages), and \code{optimization} (one row per
starting-value/optimizer attempt). Failed candidates have \code{bic = Inf}.
}
\description{
Fit a single exposure on the supplied scale. This function does not log or
shift the data. Pass \code{candidates = "norm"} for a single censored normal fit,
or multiple families for minimum-BIC selection.
}
\details{
At least two observed values are required. Nonzero observed variation is
required except for the exponential family. Positive-support families require positive observed values and
a positive cutoff when censoring is present. Failed candidates are retained
in the diagnostic table but cannot win BIC selection. If every candidate
fails, the function raises an error. Family-specific multiple starting values are retained. Optimization warnings are captured in
\code{optimization$warnings}, rather than printed during fitting.
}
\examples{
d <- nhanes_pah
marginal <- copmi_fit_margin(d$X_cens[, 1], d$ind[, 1], d$cutoffs[1],
                             candidates = "norm")
marginal$parameters
marginal$candidate_table
marginal$optimization
}
\seealso{
\code{\link[=copmi_fit_margins]{copmi_fit_margins()}}, \code{\link[=copmi_transform]{copmi_transform()}}
}
