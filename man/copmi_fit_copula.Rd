% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/copula-fit.R
\name{copmi_fit_copula}
\alias{copmi_fit_copula}
\title{Estimate the latent Gaussian copula correlation}
\usage{
copmi_fit_copula(
  object,
  max_iter = 100,
  tol = 1e-04,
  seed = 1,
  verbose = FALSE,
  lyles_control = list()
)
}
\arguments{
\item{object}{A \code{copmi_latent} object from \code{\link[=copmi_transform]{copmi_transform()}}.}

\item{max_iter}{Nonnegative integer maximum number of EM iterations. Zero
returns initialization only and sets \code{converged = FALSE}.}

\item{tol}{Positive finite convergence tolerance: the maximum absolute
elementwise change in the correlation matrix must be smaller than \code{tol}.}

\item{seed}{Nonnegative integer seed, or \code{NULL} to use and advance the current
RNG stream. Explicit seeds restore the caller's RNG state on exit.}

\item{verbose}{Whether to print initialization and EM progress.}

\item{lyles_control}{Named list with optional \code{min_obs} (default 2), \code{maxit}
(2000 per optimizer), and \code{rho_max} (1) for pairwise censored-normal initialization.
Pairwise likelihoods try L-BFGS-B then Nelder-Mead. Failed pairs
raise errors. \code{rho_max < 1} explicitly restricts censored-pair fits;
fully observed pairs use their sample correlation.}
}
\value{
A \code{copmi_copula} list containing \code{Sigma_hat}, \code{Sigma_init} (correlation
matrices), \code{converged}, \code{n_iter}, \code{em_change_history}, \code{init_diagnostics}
(including the per-pair table), \code{fallback_records}, \code{latent} (the supplied
object), \code{rng_state} (state after estimation for reproducible draws),
\code{seed}, \code{max_iter}, \code{tol}, and \code{lyles_control}.
}
\description{
Estimate pairwise censored-normal correlations for initialization, then
update the latent correlation matrix by EM using truncated-normal moments.
This step estimates the correlation model without drawing completed data.
}
\details{
Convergence refers only to the correlation-change criterion, not
convergence of the marginal optimizers or proof of a global optimum.
An indefinite finite pairwise initial matrix is projected with
\code{\link[Matrix:nearPD]{Matrix::nearPD()}}; its adjustment is recorded as \code{init_diagnostics$pd_adjustment}.
EM updates are not projected. Moment or linear-algebra failures raise errors
rather than replacing moments or adding a ridge. Unconverged fits can be
inspected but require explicit opt-in before imputation.
}
\examples{
# Use the first 100 rows for a quick help example; full data have 1330 rows.
dat <- make_lod_data(nhanes_pah$X_cens[1:100, ],
                     nhanes_pah$ind[1:100, ], nhanes_pah$cutoffs, scale = "log")
margins <- copmi_fit_margins(dat, margin_mode = "normal")
model <- copmi_fit_copula(copmi_transform(margins), max_iter = 100, seed = 1)
model$Sigma_hat
model$em_change_history
}
\seealso{
\code{\link[=copmi_impute]{copmi_impute()}}, \code{\link[=diagnostics]{diagnostics()}}
}
