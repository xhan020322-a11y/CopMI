% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data-input.R
\name{make_lod_data}
\alias{make_lod_data}
\title{Validate and construct left-censored exposure data}
\usage{
make_lod_data(X_cens, ind, cutoffs, scale = "analysis")
}
\arguments{
\item{X_cens}{Numeric matrix or data frame, with samples in rows and exposure
variables in columns. Observed cells must be finite; censored cells may be
finite or \code{NA} and are replaced by the corresponding cutoff.}

\item{ind}{Matrix or data frame of the same dimensions, containing exactly
\code{0}/\code{FALSE} (left-censored) and \code{1}/\code{TRUE} (observed). Fractional values and
missing indicators are rejected before any integer conversion.}

\item{cutoffs}{Numeric vector, one cutoff per column, in column order. A
named vector must match the column names in order. Use a finite cutoff for
each censored column; \code{NA} is allowed for a fully observed column.}

\item{scale}{One nonempty character label, such as \code{"analysis"} or \code{"log"}.}
}
\value{
A \code{copmi_lod_data} list with \code{X_cens} (double matrix), \code{ind} (integer
matrix), \code{cutoffs} (numeric vector), and \code{scale}.
Matrix dimensions and sample/variable names are preserved. If absent,
column names are generated as \code{X1}, \code{X2}, etc.
}
\description{
Construct the common input to all CopMI workflows. No log or exponential
transformation is performed: values and cutoffs must already use the same
analysis scale. The indicator, not the placeholder value, defines censoring.
}
\details{
Observed values below a finite cutoff are inconsistent with the
left-censoring model and are rejected. Observed values equal to the cutoff
are allowed. Column names must be unique and cannot be \code{.imp} or \code{.id},
which are reserved by \code{\link[=complete]{complete()}}. General missingness, right censoring,
and cell-specific cutoffs are not represented by this input contract.
}
\examples{
dat <- make_lod_data(nhanes_pah$X_cens,
                     nhanes_pah$ind, nhanes_pah$cutoffs)
print(dat)
dim(dat$X_cens)
colSums(dat$ind == 0L)
}
\seealso{
\code{\link[=copula_em_impute]{copula_em_impute()}}, \code{\link[=copmi_fit_margins]{copmi_fit_margins()}}
}
