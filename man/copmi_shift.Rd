% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data-input.R
\name{copmi_shift}
\alias{copmi_shift}
\title{Construct the SD shift used by heterogeneous margins}
\usage{
copmi_shift(x, shift_k = 3)
}
\arguments{
\item{x}{A \code{\link[=make_lod_data]{make_lod_data()}} object.}

\item{shift_k}{Positive finite multiplier of the observed standard deviation.}
}
\value{
A \code{copmi_shift} list with \code{dat} (shifted \code{copmi_lod_data}), \code{anchor},
\code{sd}, \code{sd_source}, \code{shift_multiplier},
\code{min_shifted_observed_or_cutoff}, \code{original_cutoffs}, and \code{shifted_cutoffs}.
Each vector has one element per column. Recover the input scale by adding
\code{anchor} to the shifted columns with \code{sweep(..., 2, anchor, "+")}.
}
\description{
Shift each column by \code{anchor = reference - shift_k * observed_sd} and return
both shifted values and the information needed to undo the shift.
}
\details{
The reference is the finite cutoff, or the minimum observed value
if the cutoff is \code{NA}. SD is estimated from each column's observed cells only.
An unavailable or zero SD raises an error; scales are never borrowed from
other variables. All visible shifted values must be positive. This is an
additive shift, not a log transformation. Positive-support selected families
imply values above \code{anchor}; selected normal/logistic families retain their
full real support and do not impose this extra lower bound.
}
\examples{
shifted <- copmi_shift(nhanes_pah)
shifted$anchor
restored <- sweep(shifted$dat$X_cens, 2, shifted$anchor, "+")
all.equal(restored, nhanes_pah$X_cens)
}
\seealso{
\code{\link[=copmi_fit_margins]{copmi_fit_margins()}}
}
